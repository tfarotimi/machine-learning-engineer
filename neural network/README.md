Simple Neural Net is a class that does the following:

1. An instance is created by calling the object with a dataframe and a target label.
2.   Its initialized with the target name, the features and their names, training and test, number of batches and no of epochs, and a learning rate.
3.  the initialize neurons method , creates  2 intermediate layers and an output layer,
    a.  layer 1 is a 3 by 3 area matrix, 3 is the number of features, and the number of neurons is also . layer 2 is 3 x 2, layer 1 is 2 x 1. at each state we add a relu.
    b.  the multiplication through the network begins with 10 * 3 @ 3 * 3, which is a 10 x 3 matrix, whwih after relu is appllied, gets multiplied by a 3x 2, giving a 10 x 2, and finall a 2, 1 until we have a 10 * 1 matrix that is the output layer, essentially we've continuosly combined the original 3 features into 1, going from 2 to 1, each case we have the 10 inputs, their results and activations going into the next layer.
4. so we divide the data into batches of n size, and in each epoch, we process all the batches in the data set. in each case, we multiply the inputs to the initial sets of weights in each layer and activate, until we get our 10 x 1 matrix output, then we compute the loss against the actual lvalues of the output.

5. we then find out the gradient, direction of steepest descent in the surface functionn, we do this by tkaing the derivative of the loss function, plugging in our outputs and actuals into it, and finding out the vector of the direction of steepest descent, we then multiply this by the output layer (2 * 1) , the answer, is then multiplied by the previous layer (3*2), then we multiply that by the 3*3 at each step, we are getting the values of the weighs that would have produced ,  the outputs that would have minimized the loss.. this is the the hard part.

Note: The derivative formula applied to gives us 10 by 1 matrix?, that is the smallest change in the weights that would take you in the direction of the minimum , because the output layer was a 2 x 1 matrix, we then mulitpy both to get a 10 * 2 matrix? then the prev layer is 3 * 2, so multiplying  gives us 10 * 3,and each step we have found the change to the weights that would have eventually led to the final 10 outputs that whould have minimized the loss between predictions and output , we also multiply by the activation function at each step, so in the end weve got the 10 x 3, we multiply it by the learning rate and then adjust the weights by these amounts, then we do another pass with each batch until all batches, thats 1 epoch, then we do all that for 200 epochs.
